import 'package:flutter/material.dart';

class ReportRiskScreen extends StatelessWidget {
  const ReportRiskScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Risk Report')),
      body: const Center(child: Text('Report Risk Screen')),
    );
  }
}
